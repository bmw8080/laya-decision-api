# 容器运行期依赖（Linux）。与 pyproject 的区分：
#   - pyproject 的后端依赖是 extras：`[mlx]`（macOS/Metal）或 `[torch]`；容器里装不了 mlx 也用不上；
#   - 容器走 torch 后端：torch + laya（见 Dockerfile 的 TORCH_INDEX_URL / LAYA_PIP_SPEC）。
fastapi>=0.130
uvicorn>=0.40
pydantic>=2.10
httpx>=0.28

# 说明（架构无关性）：以上 4 个都是 py3-none-any 纯 Python，x86_64 与 arm64 通吃。
# 另外两处依赖由 Dockerfile 单独装：
#   torch          —— x86_64 / aarch64 均有 manylinux wheel；走 CPU 索引（约 200MB，不带 CUDA 库）
#   上游 laya      —— py3-none-any 纯 Python，架构无关；会带出 transformers / tokenizers /
#                     safetensors / huggingface_hub / numpy（都有两架构 wheel）
# 体积量级（实测，arm64 VM 跨构建 linux/amd64）：镜像解包 1.76GB（15 层），
# `docker save | gzip -1` 交付包 902MB —— 主要来自基础镜像 ~150MB + CPU torch（解包约 500MB）
# + transformers 系 ~100MB + 权重 644MB。
# 若基础镜像换成 pytorch/pytorch:*（自带 torch）则跳过 torch 安装，总体 2–3GB。
